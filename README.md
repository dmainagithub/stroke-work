This repository contains a number of models, such as Negative Binomial, Standard Poisson, used for Stroke Analysis.

Adding notebooks and lines of code incrementally for improvement.

Important commands --
git pull --rebase origin master
git push origin master

